//
//  bestfit.c
//  Assignment 4
//
//  Created by Will Chan on 3/10/15.
//  Copyright (c) 2015 Lost Boys. All rights reserved.
//

#include "bestfit.h"

void runBestFit()
{
    /*
    Process **memory = newMainMemory(); // Gets a new block of main memory as an array of 100 process pointers 
    std::vector<Partition *> holes = getHolesInMemory(memory);  // Get and find holes in memory
    int startOfVector = 0, startOfMain = 0;
    while(startOfMain < 100)
    {
        Process *process = newProcess(); // Gets a randomly generated new process  
        startOfMain += process.getProcessSize(); // Gets the process size and increment the index of the main memory
        while(startOfVector < holes.size()) // Start adding processes in the vector 
        {
            if ( holes.at(startOfVector) == NULL && startOfVector < process.getProcessSize() ) // if the vector at that index is null ( or  0 )
            {
                holes.at(i) = process; // Adds that process into the array of that index 
                startOfVector++;  // moves the index to the start 
            }
        }
    }
    */
    return;
}
